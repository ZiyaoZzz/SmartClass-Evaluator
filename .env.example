# Admin login (test credentials; change in production)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=YOUR_ADMIN_PASSWORD

# Session signing (required in production; use a long random string)
AUTH_SECRET=your-auth-secret-at-least-32-chars

# SQLite database for quiz submissions (path relative to project root)
DATABASE_URL="file:./.data/submissions.db"
